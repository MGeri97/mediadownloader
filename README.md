# MediaDownloader

This repository will host the rebranded Nextcloud app "MediaDownloader", based on the original NCDownloader by jiaxinhuang (AGPL).

The app provides a web GUI for Aria2 and yt-dlp inside Nextcloud.

Source code will be imported from the `ncdownloader-refresh` branch of `MGeri97/ncdownloader`.
